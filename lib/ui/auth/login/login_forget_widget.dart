
import 'package:flutter/material.dart';

class LoginForgetWidget extends StatelessWidget {
  const LoginForgetWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return TextButton(onPressed: () {}, child: const Text("Password dimenticata?"));
  }
}
